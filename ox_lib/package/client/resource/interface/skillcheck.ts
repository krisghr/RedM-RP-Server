type Difficulty = 'easy' | 'medium' | 'hard' | { areaSize: number; speedMultiplier: number };

export const  = (difficulty: Difficulty | Difficulty[], inputs?: string[]) =>
  exports.ox_lib.(difficulty);
